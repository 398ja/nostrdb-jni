@param java.util.Map<String, Object> result

<div class="alert alert-success">
    <strong>${result.get("message")}</strong><br>
    Previous file size: ${result.get("previousFileSize")} bytes<br>
    Previous entries: ${result.get("previousEntries")}
</div>
